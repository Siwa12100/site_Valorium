@page "/"
@* @layout Shared.LayoutPrincipal *@

<h1>Hello, world!</h1>

@if (lArticles != null)
{
    foreach(var a in lArticles)
    {
        <div> @a.Titre</div>
        <div> @a.Description</div>
    }
}

<NavLink href="/test"> Message de test vers la page de test... </NavLink>

